__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local-only working docs (not tracked). The tracked, condensed changelog is
# CHANGES.md; CHANGELOG.md is kept locally as the detailed/internal archive.
CLAUDE.md
CHANGELOG.md

# Cygwin/MSYS crash dumps (build/runtime cruft)
*.stackdump

# Signing key material (mcpb sign --self-signed) — never commit private keys
*.pem

# Packaging outputs (build them on release; don't commit)
*.mcpb
_build_out.txt
PUBLISHING.md
_*.bat
/_*.py
/_*.txt
